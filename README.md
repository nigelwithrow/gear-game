
### Build Once
```
npm run build
```

### Watch Build
```
npm run make out/index.html
npm run watch
```

these will both build an index.html and a main.js (used in the index.html) to `out/`, so you can run a static server inside `out/` to serve those files
